<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Prescription</title>
</head>

<body>

  <form action="/Ddashboard/DmyAppointments/Prescription/<%=appid%>" method="POST" enctype="multipart/form-data">

    <label for="photo">Upload Prescription</label>

    <p><input type="file" accept="image/png,image/jpeg,image/jpg" id="file" required name="photo"
        onchange="loadFile(event)"></p>
    <p><label for="file" style="cursor: pointer;"></label>
    </p>
    <p><img id="output" width="200" /></p>

    <button type="submit">send file</button>
  </form>
  <script>
    var loadFile = function (event) {
      var image = document.getElementById('output');
      image.src = URL.createObjectURL(event.target.files[0]);
    };
  </script>


</body>

</html>